services:
  postgres:
    image: postgres:16
    container_name: medMaisFacil-db
    environment:
      POSTGRES_DB: MMF-DB
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - medfacil_data:/var/lib/postgresql/data

volumes:
  medfacil_data: